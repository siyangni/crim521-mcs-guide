{
  "name": "mcs-class-user-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Teaching-site edition of the MCS class-file user guide. Does not include the dataset.",
  "scripts": {
    "ingest": "node scripts/ingest.mjs",
    "test": "node --test scripts/*.test.mjs",
    "dev": "npm run ingest && astro dev",
    "build": "npm run ingest && astro build && node scripts/check-dist.mjs",
    "preview": "astro preview"
  },
  "engines": {
    "node": ">=20.3.0"
  },
  "dependencies": {
    "@fontsource/fraunces": "^5.2.5",
    "@fontsource/ibm-plex-mono": "^5.2.5",
    "@fontsource/source-sans-3": "^5.3.0",
    "@fontsource/source-serif-4": "^5.2.5",
    "astro": "^5.18.2",
    "marked": "^15.0.12"
  }
}
